# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rescue_pf?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    hikari:
      # ????
      pool-name: DateHikariCP
      # ???????
      minimum-idle: 5
      # ?????????????600000?10???
      idle-timeout: 180000
      # ????????10
      maximum-pool-size: 10
      # ??????????????
      auto-commit: true
      # ?????????0?????????1800000?30???
      max-lifetime: 1800000
      # ?????????30000?30??
      connection-timeout: 30000
      # ?????????????
      connection-test-query: SELECT 1

# Mybatis-plus??
mybatis-plus:
  mapper-locations: classpath*:/mapper/**Mapper.xml

server:
# ??
  port: 8081

# jwt??
jwt:
  # JWT??????
  # ?????? tokenHeader?Authorization ?key tokenHead?Bearer??? value ??????jwt?????????
  tokenHeader: Authorization
  # JWT ????????
  secret: yeb-secret
  # JWT???????60*60*24?
  expiration: 604800
  # JWT ???????
  tokenHead: Bearer

